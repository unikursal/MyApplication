#pragma once

/*For function getPosition (interface IProcessor).
   Indicates the position from which to start reading the file*/
const bool READ_WITH_BEGIN = true;
const bool READ_WITH_END = false;